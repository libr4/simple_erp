# Sample environment variables for local docker-compose
# Copy this file to .env and adjust values before running `docker compose up`.

# Backend host port (host:container)
BACKEND_PORT=5000

# Frontend host port
FRONTEND_PORT=3000

# SQL Server host port
MSSQL_PORT=1433

# SA password for SQL Server container (change for production!)
MSSQL_SA_PASSWORD=Your_password123

# (Optional) Additional configuration
# CONNECTION_STRING=Server=sqlserver,1433;Database=TargetGupy;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=True;MultipleActiveResultSets=True;
